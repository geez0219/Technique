## How to check who connect to ec2

link to that ec2 instance and 
>>> whowatch (this will check the user using usaual ssh connection)
>>> ps aux | grep extension (this will check the user using vscode ssh connection)
